import { connect } from "react-redux"

function Navbar(){
    return(
        <nav>
            navbar
        </nav>
    )
}

const mapStateToProp=state=>({

})

export default connect(mapStateToProp, {

})(Navbar)